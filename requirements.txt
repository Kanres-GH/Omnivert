# Runtime
yt-dlp>=2025.1.1
pywebview>=6.0          # native window hosting the web UI (uses Edge WebView2 on Windows)

# Build-time (only needed to (re)build the .exe and icon)
pillow>=10.0.0
pyinstaller>=6.0.0
